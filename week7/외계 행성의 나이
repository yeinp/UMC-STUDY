#include <string>
#include <vector>
#include <string>

using namespace std;

string solution(int age) {
    string answer = "";
    string age_s = to_string(age);
    for (int i = 0; i<age_s.size(); i++) {
        answer+=age_s[i] + 49;
    }
    return answer;
}
